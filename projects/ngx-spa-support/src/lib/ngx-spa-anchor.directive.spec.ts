import { NgxSpaAnchorDirective } from './ngx-spa-anchor.directive';

describe('NgxSpaAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxSpaAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
