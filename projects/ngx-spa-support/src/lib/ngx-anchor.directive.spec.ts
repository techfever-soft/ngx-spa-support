import { NgxAnchorDirective } from './ngx-anchor.directive';

describe('NgxAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
